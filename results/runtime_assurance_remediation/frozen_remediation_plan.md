# Frozen remediation plan

Written before remediation outcome inspection on branch `analysis/runtime-assurance-remediation`.

Scope: evaluate whether the NF-SQI work can support a substantially reframed engineering manuscript as a runtime signal-admissibility monitor, reward-delivery interlock, measurement-assurance layer, and traceable accept-withhold supervisor.

Non-goals: artifact classification, artifact removal, clinical safety certification, neurofeedback efficacy, and decoder-performance enhancement.

Prespecified analyses:

1. Calibration remediation: compare OVERLAP_50, NONOVERLAP, and BLOCK_SUBSAMPLE baseline-window strategies; EMPIRICAL, HARRELL_DAVIS, and SHRUNK_SESSION threshold estimators; durations 30, 60, 90, 120, 180, 240, 300 s and full baseline where available.
2. Sequential readiness: use rest-only threshold convergence at cumulative checkpoints; evaluate 10% primary and 5% sensitivity threshold-change rules with two consecutive ready checkpoints.
3. Scheduler remediation: preserve 0.5 s monitor evaluation and evaluate only S0-S6 prespecified display schedulers.
4. External baseline: audit Riemannian Potato feasibility; if unavailable or invalid in the local environment, implement and label a robust covariance-distance monitor without calling it RPF.
5. Controlled degradation: use copies of real windows initially accepted by canonical Gate C; do not alter raw EDFs; evaluate D0-D13 classes with prespecified severity levels.
6. Montage reproducibility: audit E36/E104/E128 mapping and expanded ROI reproducibility from available metadata/code.
7. Canonical runtime implementation/configuration: create a v2 machine-readable runtime configuration and reusable runtime-assurance modules if needed.
8. Figures: generate new scientific figures only in R from frozen CSV outputs; no titles.
9. Author package: produce numerical tables, limitations, claim-evidence matrix, manuscript-change map, and readiness report without writing manuscript prose.

The downstream decoder boundary result is preserved and must not be optimized away.

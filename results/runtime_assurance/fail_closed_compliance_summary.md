# Fail-closed compliance

All controlled invalid-input cases returned `withhold=true`, `invalid_input=true`, and an explicit reason. Channel/sample-rate cases are enforced at the runtime input adapter boundary and are not claims about natural EEG artifacts.
